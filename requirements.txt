# 沃林学生信息管理系统 —— Python 依赖清单
# 安装方式：pip install -r requirements.txt
# 说明：带 == 的是当前环境验证过的精确版本；
#       末尾"待补充版本"的三个包当前环境未安装，先不锁版本，安装时会自动装最新版，
#       装好后建议用 `pip freeze` 回填精确版本，保证团队环境一致。

# ==================== Web 框架 ====================
fastapi==0.129.0          # Web 框架，提供路由、依赖注入、自动文档等
starlette==0.52.1         # FastAPI 底层 ASGI 框架（中间件、请求/响应都基于它）
uvicorn==0.24.0           # ASGI 服务器，用来跑 FastAPI 应用

# ==================== 数据校验 ====================
pydantic==2.10.3          # 请求/响应数据建模与校验（scheme 层大量使用）

# ==================== 数据库 ====================
SQLAlchemy==2.0.39        # ORM，负责模型定义与数据库操作
PyMySQL                   # MySQL 驱动（database.py 用 mysql+pymysql 连接）—— 待补充版本

# ==================== 配置 ====================
python-dotenv==1.1.0      # 读取 .env 配置文件（config.py 使用）

# ==================== 第三方接口 / 大模型 ====================
requests==2.32.3          # 调用腾讯地图天气/地理编码等 HTTP 接口
openai                    # 调用 DeepSeek（OpenAI 兼容接口）—— 待补充版本
dashscope                 # 阿里云通义万相文生图 —— 待补充版本
